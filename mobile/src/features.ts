// Feature flags (mobile). Flip to turn a feature's UI on/off without deleting
// its code — the screens/API stay in place so it can be re-enabled later.
//
// 채팅(1:1 메시지)은 서버 상시 운영 부담 때문에 지금은 꺼둔다. 채팅 화면·API는
// 그대로 보관돼 있고, 이 값을 true로 바꾸면 하단탭·상품상세 진입점이 돌아온다.
export const CHAT_ENABLED = false;
