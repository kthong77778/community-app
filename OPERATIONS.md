# 혼자 운영 체크리스트 (댕냥마을)

관리형 호스팅 기준으로 **한 사람이** 서버를 유지하기 위한 최소 루틴.
배포 방법은 `DEPLOY.md`, 아키텍처는 `CLAUDE.md` 참고.

## 0. 배포 직후 1회 세팅
- [ ] `AUTH_SECRET`을 충분히 긴 랜덤 값으로 설정했는가 (미설정 시 개발용 기본값 → 위험)
- [ ] `src/lib/accounts.json`의 계정/비밀번호를 실제 값으로 변경 (기본값 그대로 두지 말 것)
- [ ] `/api/health`가 200을 반환하는지 확인
- [ ] 데이터 영속성 확인: 글 하나 쓰고 **재배포 후에도 남아있는지** 테스트
  (SQLite면 디스크 볼륨, Postgres면 `DATABASE_URL` 연결 확인)
- [ ] 업로드 테스트: 상품 사진 올리고 재배포 후에도 보이는지 (`UPLOAD_DIR` 볼륨)
- [ ] 도메인 + HTTPS 연결 (관리형은 보통 자동)

## 1. 백업 (제일 중요)
데이터가 날아가면 끝이다. **자동 백업을 반드시 켠다.**
- **Postgres(권장)**: Neon/Supabase 등 관리형은 자동 백업(PITR) 제공 → 켜고 보존기간 확인
- **SQLite**: 파일 하나(`community.db`)라 백업이 쉽다. 주기적으로 디스크의
  `community.db`를 어딘가로 복사(호스트 스냅샷, 또는 cron으로 오브젝트 스토리지 업로드).
  - 즉석 백업: `sqlite3 community.db ".backup backup-$(date +%F).db"` (WAL 안전)
- **업로드 이미지**: 오브젝트 스토리지(R2/S3)면 자체 내구성. 영속 디스크면 위 스냅샷에 포함.
- [ ] 백업이 **실제로 복원되는지** 최소 1회 테스트 (복원 안 되는 백업은 백업이 아님)

## 2. 모니터링 (알림만 받으면 됨)
- [ ] **업타임 모니터**: UptimeRobot/BetterStack 무료 티어로 `/api/health`를 5분마다 체크,
  다운 시 이메일/문자 알림
- [ ] **호스트 대시보드**: 관리형(Render/Railway)의 CPU·메모리·재시작 로그를 가끔 확인
- [ ] **에러 로그**: 호스트 로그 탭. 필요하면 Sentry 무료 티어 연동
- [ ] **디스크/DB 용량**: 사용량이 한도에 가까워지는지 월 1회 확인

## 3. 업데이트 / 배포
- 코드: 기본 브랜치에 push → 관리형이 자동 배포(autoDeploy). 배포 후 `/api/health` + 주요 화면 눈으로 확인
- 검증: 배포 전 로컬에서 `npm run build` · `npm test`(현재 43개) 통과 확인
- 의존성 보안: 월 1회 `npm audit` 확인, `npm audit fix`로 패치. 메이저 업그레이드는 신중히
- 모바일 앱: 스토어 심사·업데이트는 웹과 별개 주기 (변경 잦으면 부담 → 꼭 필요할 때만)

## 4. 보안
- [ ] `AUTH_SECRET`, `DATABASE_URL` 등 비밀은 **환경변수로만**, 저장소에 커밋 금지
- [ ] `accounts.json`은 하드코딩 평문이다. 계정 수가 적을 때만 적합.
  사용자에게 계정을 열어줄 거면 진짜 회원가입+비밀번호 해시로 전환 필요(로드맵)
- [ ] 관리자 계정 비밀번호는 길고 고유하게
- [ ] 의존성 취약점(`npm audit`) 주기 점검

## 5. 사고 대응 / 롤백
- 배포가 앱을 깨뜨리면: 관리형 대시보드에서 **직전 배포로 롤백**(원클릭) 또는
  문제 커밋 revert 후 재배포
- DB가 이상해지면: 최근 백업에서 복원 (그래서 1번 백업이 중요)
- 장애 시 우선순위: ① 서비스 복구(롤백) → ② 원인 파악 → ③ 재발 방지

## 6. 콘텐츠 관리 (사용자 늘면 서버보다 큰 부담)
- 신고/숨김 기능으로 스팸·부적절 게시물 처리 (관리자 로그인 → 신고함 확인 → 숨김)
- 정기적으로 신고함(`/admin/reports`)을 확인하는 습관
- 불법/유해 콘텐츠는 빠르게 숨김 처리

## 7. 비용 감각
- 소규모: 관리형 웹 + 무료/저가 DB로 **월 $0~$20** 수준
- 늘어나면: 트래픽(대역폭), DB 용량, 이미지 스토리지 순으로 비용 증가
- 무료 티어는 종종 "일정 시간 미사용 시 슬립" → 첫 요청이 느릴 수 있음(업타임 모니터가 깨워주기도 함)

## 정기 점검 요약
- **주간**: 업타임 알림 이상 없었는지, 신고함 확인, 로그 훑기
- **월간**: 백업 복원 테스트, `npm audit`, 디스크/DB 용량, 비용 확인
